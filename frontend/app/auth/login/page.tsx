import PublicRoute from "@/app/components/publicRoute";
import FormWrapper from "../form_wrapper";

const Page = () => {
  return (
    <PublicRoute>
      <FormWrapper />
    </PublicRoute>
  );
};

export default Page;
